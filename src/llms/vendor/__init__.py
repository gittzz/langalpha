"""Vendored third-party packages.

Self-contained copies of external packages we want to exercise inside langalpha
before depending on them from PyPI. Each subpackage mirrors its upstream source
and is maintained upstream — see the local ``README.md`` for provenance and the
(small) in-tree divergences. Excluded from our lint rules in ``pyproject.toml``.
"""
